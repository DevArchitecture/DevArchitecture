# GitHub Pages placeholder: repo has Markdown under `docs/` only. Replace with a static site
# build (e.g. DocFX) when ready.
#
# To enable: Repository → Settings → Pages → Source = GitHub Actions, then add a job that
# produces `dist/` or `_site/` and use actions/upload-pages-artifact + actions/deploy-pages.
name: pages

on:
  workflow_dispatch:

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Pages not configured
        run: |
          echo "::notice::Pages workflow is a placeholder. Add a static site build and deploy with actions/upload-pages-artifact + actions/deploy-pages."
